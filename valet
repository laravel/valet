#!/usr/bin/env bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

if [ ! -f "$DIR/valet.php" ]
then
    OLDPWD=$PWD
    cd $DIR/../laravel/valet
    DIR=$(pwd)
    cd $OLDPWD
fi

if [[ $1 = "install" ]]
then
    sudo php $DIR/valet.php $@
elif [[ $1 = "share" ]]
then
    bash $DIR/share-fetch-url.sh &
    $DIR/bin/ngrok http -host-header=rewrite ${PWD##*/}.dev:80
else
    php $DIR/valet.php $@
fi

